# lo
REST API на Go для управления сущностями Task с асинхронным логированием действий через канал.  Функционал: - GET /tasks — получить список задач (фильтрация по статусу) - GET /tasks/{id} — получить задачу по ID - POST /tasks — создать задачу
